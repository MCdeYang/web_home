## 问题分析

### 1. 设置页面缺失功能
- 缺少 WiFi 设置（SSID、密码、连接/断开）
- 缺少内网穿透设置（Frp/Ngrok 配置、启动/停止）
- 天气设置是新增的，但可能后端不支持

### 2. 主题切换问题
- 只更换了背景，按钮等组件风格未同步修改
- 需要确保所有主题都有完整的组件样式

## 修复方案

### 1. 恢复设置页面功能
在 settings.html 中添加：
- WiFi 设置卡片：显示当前连接、SSID输入、密码输入、连接/断开按钮
- 内网穿透设置卡片：服务器地址、Token、启动/停止按钮、状态显示

### 2. 完善主题CSS
确保每个主题的 CSS 文件都包含：
- 按钮样式（.theme-btn, .theme-btn-primary 等）
- 输入框样式
- 开关样式
- 卡片样式
- 所有组件的悬停/激活状态

### 3. 修改策略
- 保留界面风格选择功能
- 添加 WiFi 和内网穿透设置卡片
- 修复天气设置（如果后端支持）或移除
- 统一所有主题的按钮类名，确保切换时样式正确应用

### 4. 后端 API 确认
根据 routes.c，后端支持：
- GET/PUT /settings/wifi - WiFi设置
- GET/PUT /settings/public - 内网穿透设置
- GET/POST /settings/change-password - 修改密码

请确认此修复方案后，我将开始实施具体的修改。