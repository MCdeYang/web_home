## 修改计划

### 1. 修复字符遮盖问题

**文件**: `login.html`

* 添加密码可见性切换功能（显示/隐藏密码按钮）

* 在密码输入框右侧添加眼睛图标按钮

* 修复用户名图标与用户名汉字；密码图标密码汉字的相互遮盖问题

### 2. 根据主题切换登录界面样式

**文件**: `login.html`

* 动态加载用户选择的主题 CSS 文件

* 在页面加载时读取 localStorage 中的主题设置

* 应用对应的主题 class 到 body

* 为每个主题添加登录页专用样式

### 3. 为各主题添加登录页样式

**文件**:

* `galaxy-theme.css` (已存在，需要添加登录页样式)

* `sky-cartoon-theme.css` (添加登录页样式)

* `dark-luxury-theme.css` (添加登录页样式)

* `colorful-theme.css` (添加登录页样式)

* `moonlight-theme.css` (添加登录页样式)

* `chinese-knot-theme.css` (添加登录页样式)

每个主题添加:

* `.theme .login-panel` - 登录面板样式

* `.theme .login-btn` - 登录按钮样式

* `.theme .form-input:focus` - 输入框聚焦样式

* `.theme .login-title` - 标题渐变

彩色活力与蓝色卡通的system功能块的颜色过于深 更改颜色

