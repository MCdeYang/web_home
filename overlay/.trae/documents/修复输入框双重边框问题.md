## 问题分析
输入框出现了双重边框/圆角效果，这是因为：
1. `.form-group` 有边框和圆角
2. `.form-input` 可能有默认样式或继承样式

## 解决方案
1. 给 `.form-input` 添加明确的 `border: none !important` 
2. 添加 `box-shadow: none` 移除可能的阴影
3. 确保 `background: transparent` 没有覆盖问题
4. 检查是否有其他CSS文件（galaxy-theme.css）在影响输入框样式